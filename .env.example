# Hintily desktop-safe configuration
HINTILY_ENV=development
HINTILY_SUPABASE_URL=https://your-project.supabase.co
HINTILY_SUPABASE_ANON_KEY=your_publishable_anon_key
HINTILY_OAUTH_CALLBACK_URL=http://127.0.0.1:54321/auth/callback
HINTILY_WEBSITE_URL=https://hintily.app
HINTILY_SUPPORT_URL=https://hintily.app/support

# Never put SUPABASE_SERVICE_ROLE_KEY, DODO_API_KEY, managed provider keys,
# or webhook secrets in this desktop .env file. Configure them as server-side
# Supabase Edge Function secrets.
# Server-only examples (set with `supabase secrets set`, never in this file):
# DODO_PAYMENTS_API_KEY, DODO_PAYMENTS_WEBHOOK_KEY,
# DODO_PAYMENTS_ENVIRONMENT, DODO_PRODUCT_MAP,
# HINTILY_CHECKOUT_RETURN_URL, HINTILY_CHECKOUT_CANCEL_URL,
# DEEPGRAM_API_KEY,
# HINTILY_MANAGED_OPENAI_API_KEY, HINTILY_MANAGED_OPENAI_MODEL,
# HINTILY_MANAGED_GROQ_API_KEY, HINTILY_MANAGED_GROQ_MODEL,
# HINTILY_MANAGED_CLAUDE_API_KEY, HINTILY_MANAGED_CLAUDE_MODEL,
# HINTILY_GEMINI_API_KEY, HINTILY_MANAGED_GEMINI_MODEL,
# HINTILY_GOOGLE_CALENDAR_CLIENT_ID, HINTILY_GOOGLE_CALENDAR_CLIENT_SECRET.

# Public OAuth client identifier only. The matching secret belongs in the
# Hintily Calendar Edge Function secrets listed above.
HINTILY_GOOGLE_CALENDAR_CLIENT_ID=your_google_calendar_client_id_here

# BYOK and local Ollama are deliberately unavailable in Hintily production.
# For isolated legacy development only (never ship these flags):
# HINTILY_ENABLE_LEGACY_BYOK_DEV=true
# HINTILY_ENABLE_LEGACY_BUSINESS_DEV=true

# ── Hindsight long-term memory (OPTIONAL, default disabled) ───────────────────
# Natively works fully WITHOUT Hindsight. To enable optional long-term memory
# (cross-meeting recall, "what did we discuss last time?"), you must (1) install
# the client `npm install @vectorize-io/hindsight-client`, (2) run a Hindsight
# server (see docs/HINDSIGHT_LOCAL_SETUP.md — needs Postgres+pgvector + an LLM key),
# and (3) set the vars below + enable the NATIVELY_HINDSIGHT_* feature flags.
# With these unset, the memory provider is a no-op and nothing changes.
# READ BY THE APP: HINDSIGHT_BASE_URL, HINDSIGHT_API_KEY, HINDSIGHT_TIMEOUT_MS
# (consumed by MeetingPersistence → LongTermMemoryService.fromFlags). The actual
# ON/OFF gates are the NATIVELY_HINDSIGHT_* feature flags, not these.
HINDSIGHT_BASE_URL=http://localhost:8888
HINDSIGHT_API_KEY=
HINDSIGHT_TIMEOUT_MS=800
# ILLUSTRATIVE ONLY (not currently read by the app — retain is always async/queued,
# and the provider is selected by the NATIVELY_HINDSIGHT_* flags, not by these):
NATIVELY_MEMORY_PROVIDER=disabled
HINDSIGHT_RETAIN_ASYNC=true
